{
  "name": "dietician-babu-rules-tests",
  "private": true,
  "scripts": { "test": "firebase emulators:exec --project demo-dietician-babu --config ../firebase.json --only firestore,storage \"node --test rules.test.js\"" },
  "devDependencies": { "@firebase/rules-unit-testing": "^5.0.0", "firebase": "^12.0.0", "firebase-tools": "^15.0.0" }
}
